import {buildGoldenArchitecture,createDefaultGoldenArchitectureState} from "./goldenArchitecture";import {recordDekoBrainEvent} from "./goldenArchitectureEvents";import {calculateEchoScore} from "./echoScore";import type {DekoBrainLearningState,ProductDNA} from "./learning";
function assert(condition:boolean,message:string){if(!condition)throw new Error(message)}const now="2026-01-01T00:00:00.000Z";
const dna:ProductDNA={id:"dna-1",mediaFingerprint:"media-1",version:3,category:"candle",confidence:78,echoScore:6,sceneComplexity:"medium",sourceType:"uploaded",protectedFeatures:["flame","wick","shadow","fineEdges"],preferredSettings:{backgroundSensitivity:62,edgeSoftness:22,detailProtection:90,shadowMode:"keep"},userConfirmedCategory:true,resultApproved:true,createdAt:now,updatedAt:now,history:[{version:1,timestamp:now,reason:"created",echoScore:0},{version:2,timestamp:now,reason:"categoryCorrected",newCategory:"candle",previousCategory:"unknown",echoScore:3},{version:3,timestamp:now,reason:"resultApproved",echoScore:6}]};
const learning:DekoBrainLearningState={schemaVersion:1,productDna:[dna],events:[{id:"e1",mediaFingerprint:"media-1",timestamp:now,predictedCategory:"unknown",correctedCategory:"candle",predictionConfidence:30,userConfirmedCategory:true,source:"userConfirmation",sceneComplexity:"medium",riskLevel:"medium",protectedFeatures:dna.protectedFeatures,settings:dna.preferredSettings,resultApproved:false}],knowledgeTree:{candle:{category:"candle",confirmations:1,corrections:1,approvedResults:1,preferredSettings:dna.preferredSettings,protectedFeatureCounts:{flame:1},echoScore:6,updatedAt:now}}};
let stored=createDefaultGoldenArchitectureState();stored=recordDekoBrainEvent(stored,{type:"categoryCorrected",productId:dna.id,imageId:"media-1",payload:{category:"candle"}});stored=recordDekoBrainEvent(stored,{type:"categoryCorrected",productId:dna.id,imageId:"media-1",payload:{category:"candle"}});assert(stored.events.length===1,"golden event deduplication");const state=buildGoldenArchitecture({hasImage:true,analyzed:true,dna,learning,warningCount:0,events:stored.events});assert(state.stages.length===7&&state.stages[0].status==="ready","seven stages and perception");assert(state.brainCells.some(cell=>cell.id==="cell:candle:flame"),"stable protection cell");assert(state.evolution.length===3,"DNA evolution only from real history");assert(calculateEchoScore({confidence:78,confirmations:1,corrections:1,approvedResults:1,memoryReliability:60,sceneComplexity:"medium",warningCount:0,dnaStable:true})>50,"deterministic echo score");
