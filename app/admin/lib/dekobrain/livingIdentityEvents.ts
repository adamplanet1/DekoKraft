export type LivingIdentityEventType="identity-created"|"identity-matched"|"identity-match-corrected"|"identity-match-rejected"|"color-variant-added"|"size-variant-added"|"capture-variant-added"|"identity-updated"|"identity-approved";export type LivingIdentityEvent={id:string;deduplicationKey:string;type:LivingIdentityEventType;identityId:string;mediaFingerprint:string;createdAt:string};const KEY="dekokraft.dekobrain.living-identity-events.v1";export function loadLivingIdentityEvents():LivingIdentityEvent[]{if(typeof window==="undefined")return[];try{const value=JSON.parse(localStorage.getItem(KEY)??"[]");return Array.isArray(value)?value:[]}catch{return[]}}export function recordLivingIdentityEvent(input:Omit<LivingIdentityEvent,"id"|"createdAt"|"deduplicationKey">){const events=loadLivingIdentityEvents(),key=`${input.type}:${input.identityId}:${input.mediaFingerprint}`;if(events.some(event=>event.deduplicationKey===key))return events;const event={...input,id:`living:${key}`,deduplicationKey:key,createdAt:new Date().toISOString()};const next=[...events,event].slice(-500);try{localStorage.setItem(KEY,JSON.stringify(next))}catch{}return next}
